তোমার কোডে `setDarkMode` কাজ করছে না কারণ:

### ❌ ভুল টাইপো হয়েছে:

```js
const [darkMode, setDartMode] = useState(true);
```

তুমি `setDarkMode` না লিখে লিখেছো `setDartMode` (মানে spelling mistake)।  
তারপর এই ভুল নাম `setDartMode` context-এ পাঠিয়ে দিয়েছো:

```js
<ThemeContext.Provider value={{ darkMode, setDartMode }}>
```

এখন তুমি যখন `useTheme()` থেকে এটি নিচ্ছো:

```js
const { darkMode, setDarkMode } = useTheme();
```

তখন `setDarkMode` undefined, কারণ `ThemeContext.Provider` থেকে তো সেই নামে value পাঠানোই হয়নি।

---

### ✅ সমাধান:

`ThemeProvider` এ spelling ঠিক করে দাও:

```js
const [darkMode, setDarkMode] = useState(true);

return (
  <ThemeContext.Provider value={{ darkMode, setDarkMode }}>
    {children}
  </ThemeContext.Provider>
);
```

---

### 🔄 অতিরিক্ত টিপস:

**React DevTools** দিয়ে তোমার context values গুলো রিয়েল টাইমে চেক করতে পারো — এতে এমন ভুল ধরা পড়বে আরও সহজে।

আরো কিছু ভুল বা সমস্যা থাকলে জানাও, দেখে দেই!
