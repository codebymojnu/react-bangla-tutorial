Call Stack (কল স্ট্যাক) — রাঁধুনির রান্না করার তালিকা

ধরেন:
আপনি একজন রাঁধুনি। আপনার কাছে অর্ডার আসে একটার পর একটা। আপনি প্রথম অর্ডারটা নেন, কিন্তু রান্নার মাঝে যদি নতুন কোনো ছোট কাজ (যেমন: পানি ফুটানো) লাগে, তাহলে আগে সেই ছোট কাজটা শেষ করেন। তারপর আবার মূল রান্নায় ফিরে আসেন।

JavaScript ঠিক এভাবেই কাজ করে।
যেটা আগে কল হয়, সেটা নিচে থাকে, আর যেটা পরে কল হয়, সেটা উপরে আসে।
উপরের কাজ না শেষ হওয়া পর্যন্ত নিচের কাজ শুরু হয় না।

Example:

```jsx
function cookRice() {
  boilWater();
  console.log("Rice cooked");
}
function boilWater() {
  console.log("Water boiled");
}
cookRice();
```

১ : `cookRice()` কল হলো → স্ট্যাকে ঢুকল
২: তার ভেতরে `boilWater()` কল হলো → উপরে এল
৩: `console.log("Water boiled")` প্রিন্ট হলো
৪: `boilWater()` শেষ হয়ে → স্ট্যাক থেকে বের হয়ে গেলো
৫: `console.log("Rice cooked")` প্রিন্ট হলো
৬: `cookRice()` শেষ হয়ে → এইটাও স্ট্যাক থেকে বের হয়ে গেলো

আসল কথা হইল

JavaScript একসাথে একটা কাজই করে।
প্রথমে যেটা কল হয়, সেটা যদি নতুন কোনো কাজের (ফাংশনের) প্রয়োজন হয়, তাহলে সেই নতুন কাজটা আগে শেষ করে।
উপরের কাজ শেষ না হলে নিচের দিকে ফেরত আসা যায় না।

ধন্যবাদ ভুল হলে ক্ষমার দৃষ্টি তে দেখবেন।😅
