অবশ্যই! চলো, **hoisting**–টা একেবারে সহজে **কোড দিয়ে** ধাপে ধাপে বুঝি।

---

## ✅ উদাহরণ ১: `var` এর hoisting

```js
console.log(x); // Output: undefined
var x = 10;
console.log(x); // Output: 10
```

### ব্যাখ্যা:

JavaScript ভিতরে ভিতরে এটাকে এমন ভাবে:

```js
var x; // hoisted
console.log(x); // undefined
x = 10;
console.log(x); // 10
```

➡️ `x` কে উপরে নিয়ে নেয়, কিন্তু ভ্যালু `10` পরে দেয়। তাই উপরে `undefined` হয়।

---

## ❌ উদাহরণ ২: `let` দিয়ে করলে

```js
console.log(y); // ❌ Error: Cannot access 'y' before initialization
let y = 20;
```

➡️ এখানে `y` উপরে যায় ঠিকই, কিন্তু **ব্যবহার করার অনুমতি দেয় না** আগে। তাই Error!

---

## ✅ উদাহরণ ৩: Function Declaration

```js
greet(); // Output: Hello, Mojnu!

function greet() {
  console.log("Hello, Mojnu!");
}
```

➡️ এখানে পুরো ফাংশনটাই hoist হয়ে উপরে যায়, তাই আগে কল করলে চলে।

---

## ❌ উদাহরণ ৪: Function Expression

```js
sayHi(); // ❌ Error: sayHi is not a function

var sayHi = function () {
  console.log("Hi!");
};
```

➡️ এখানে `sayHi` শুধু `var` হিসেবে উপরে ওঠে, মানে:

```js
var sayHi; // hoisted
sayHi(); // ❌ Error: sayHi is not a function
sayHi = function () {
  console.log("Hi!");
};
```

---

## 🔁 মিলিয়ে দেখো টেবিল:

| জিনিস                | Hoisting হয়? | আগে ব্যবহার করলে কী হয়?       |
| -------------------- | ------------ | ----------------------------- |
| `var` variable       | হয়           | `undefined`                   |
| `let`/`const`        | হয়           | ❌ Error (TDZ এর কারণে)       |
| Function Declaration | হয়           | ✅ আগেই ব্যবহার করা যায়       |
| Function Expression  | হয় (var হয়)  | ❌ Error, কারণ ভ্যালু পরে আসে |

---

## 🔚 শেষ কথা:

> Hoisting মানে — JavaScript কোড চালানোর আগে `var` আর Function Declaration গুলোকে উপরে নিয়ে যায়।

---

তুমি চাইলে আমি একই জিনিস বাংলায় চিত্র এঁকে বুঝিয়ে দিতে পারি। বলো, তাহলে একটা ডায়াগ্রাম বানিয়ে দিই? 😊
