React-এ **"omitted"** মানে হলো কোনো কিছু **ইচ্ছাকৃতভাবে বা ভুলে বাদ দেওয়া হয়েছে** — সাধারণত কোনো property, state, props, অথবা JSX-এর অংশ।

### 🟦 উদাহরণ ১: Props থেকে কোনো মান বাদ দেওয়া (omitted props)

```jsx
const UserCard = ({ name, email }) => (
  <div>
    <h2>{name}</h2>
    <p>{email}</p>
  </div>
);

// এখানে 'age' props omitted হয়েছে (মানে: দেওয়া হয়নি)
<UserCard name="Mojnu" email="mojnu@example.com" />;
```

🔸 এখানে `age` যদি প্রয়োজন হতো, কিন্তু আমরা দেইনি, তাহলে সেটা **omitted** হয়েছে।

---

### 🟦 উদাহরণ ২: Object destructuring-এ omit করা

```jsx
const user = {
  name: "Mojnu",
  email: "mojnu@example.com",
  password: "secret123",
};

const { password, ...publicData } = user;

// এখানে password omitted করা হয়েছে
```

🔸 এখানে `password` বাদ দিয়ে `publicData`-তে শুধু `name` ও `email` রাখা হয়েছে।

---

### 🔍 সংক্ষেপে:

React-এ **"omitted"** মানে হলো:
👉 কোনো props, state, বা JSX উপাদান **বাদ দেওয়া হয়েছে** — হয়তো নিরাপত্তা, সরলতা, বা প্রয়োজন না থাকায়।

আরও কোনো নির্দিষ্ট React concept বুঝতে চান কি?
